// import Svelvet from './container/views/Svelvet.svelte';
// export default Svelvet;
import DeleteMe from './deleteMe.svelte';
export default DeleteMe;

//export * from './types';

// From the 5.0 team
// not sure lib needs to stay in the repo anymore
// but all tests are currently running off this lib folder, so tests would need to be re-written
// these comments are for the next team so they know from the jump lib is currently only used for testing
// all instances of diagrams on the Svelvet webiste are running of the installation in node_modules
